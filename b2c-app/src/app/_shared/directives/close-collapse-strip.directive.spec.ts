import { CloseCollapseStripDirective } from './close-collapse-strip.directive';

describe('CloseCollapseStripDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseCollapseStripDirective();
    expect(directive).toBeTruthy();
  });
});
